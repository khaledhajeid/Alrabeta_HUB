{
  "name": "alrabeta-hub",
  "private": true,
  "version": "0.0.0",
  "description": "Repo-root tooling only (git hooks, commit linting) — the actual app lives in apps/web.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  }
}
